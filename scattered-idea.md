# Scattered Ideas

Raw ideas dump. Not for implementation yet — just capture and evaluate.

---

| # | Idea | Status | Notes |
|---|---|---|---|
| 1 | 100% free + open source | planned | Already the direction. |
| 2 | Open source = trojan risk? | not a concern | Repo is just Dart/Flutter source. Risk is APK signing, not the repo. |
| 3 | Donation tiers ($1 / $5 / $10+) | planned | Saweria link in About screen — Indonesian standard, no in-app billing needed. |
| 4 | Wake-up puzzle (must solve before phone is usable) | backlog | Fun options: **Simon Says tap sequence**, **emoji memory match**, **word scramble**, **reaction tap**. No math. Simon Says is closest to LinkedIn games vibe. Fits inside Sleep Mode / Accessibility Service plan. |
| 5 | Donor-only app lock (15-min social media detox) | not doable | No server = can't verify donations. Can't gate features without an account system. |
Add widget to count or completed the task, so people can do it directly rather than open the app.
Need to live-time update to all data.
Sync data across the devices, sync used email or gmail/apple account.
- Make a notes for to add in medicine (to log/optional) so if something happened we can log it for that day, also the log will appear on pdf exported.
- Sleep Mode, what if we just make it more sederhana, make it:
if it on window sleep time and phone doesn't active for 10 minutes = Sleep Mode.
After Sleep Mode = on, morning gate = on, you need to open the phone with morning gate.
Why my water reminder not fired today, can you check it?
Also why habit fired alarm too rather than just notif.

